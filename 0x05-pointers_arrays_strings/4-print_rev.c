#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * print_rev - Prints the reverse of a string
 * @s: string variable 's'
 *
 * Return: Always (0)
 */

void print_rev(char *s)
{
	void print_rev(char *s)
{
	int i = 0;

	while (s[i])
		i++;
	while (i--)
		_putchar(s[i]);
	_putchar('\n');
}
}
